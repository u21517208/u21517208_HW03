@model IEnumerable<u21517208_HW03.Models.FileModel>

@{
    ViewBag.Title = "Videos";
}

<h2>Videos</h2>

<!--creates space for videos to be uploaded n downloaded n deleted-->

<div class="row">

    @foreach (var item in Model)
    {
        <div class="col-md-4">
            <div class="thumbnail">
                <a href="~/Media/Videos/@item.FileName" data-fancybox="gallery" data-caption="@item.FileName">
                    <video width="320" height="240" autoplay muted>
                        <source src="~/Media/Videos/@item.FileName" type="video/mp4" />
                    </video>

                </a>
            </div>

            <table>
                <tr>
                    <td>
                        <!--shows the file name under video-->
                        <p>@item.FileName</p>
                    </td>
                    <td>
                        <button class="btn btn-success">
                            @Html.ActionLink("Download", "Download", new { FileName = item.FileName })
                        </button>
                        <button class="btn btn-danger">
                            @Html.ActionLink("Delete", "Delete", new { FileName = item.FileName })
                        </button>
                    </td>
                </tr>
            </table>
        </div>
    }
</div>

<script>
    $(document).ready(function ($) {
        $(".gallery a").fancybox();
    });
</script>